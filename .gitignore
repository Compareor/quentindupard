# Local working artefacts that must never reach the public deploy.
# Everything committed here ends up served by Cloudflare Pages, so
# anything analytical or internal stays out of git entirely.
seo-audit/
.DS_Store
